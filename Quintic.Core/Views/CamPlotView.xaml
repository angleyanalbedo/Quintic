<UserControl x:Class="Quintic.Wpf.Views.CamPlotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:oxy="http://oxyplot.org/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <ControlTemplate x:Key="DarkTrackerTemplate">
            <oxy:TrackerControl Position="{Binding Position}" LineExtents="{Binding PlotModel.PlotArea}">
                <oxy:TrackerControl.Background>
                    <SolidColorBrush Color="#F01A1A1A"/>
                </oxy:TrackerControl.Background>
                <oxy:TrackerControl.BorderBrush>
                    <SolidColorBrush Color="#3498DB"/>
                </oxy:TrackerControl.BorderBrush>
                <oxy:TrackerControl.Content>
                    <TextBlock Text="{Binding}" Margin="8" Foreground="#DDDDDD" FontSize="12"/>
                </oxy:TrackerControl.Content>
            </oxy:TrackerControl>
        </ControlTemplate>

        <Style TargetType="ContextMenu">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Grid.IsSharedSizeScope" Value="true"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{StaticResource Brush.Control.Background}" BorderThickness="1" BorderBrush="{StaticResource Brush.Control.Border}" Padding="2">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border" Background="Transparent" Padding="6,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20" SharedSizeGroup="Icon"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Checkmark for IsCheckable items -->
                                <Path x:Name="CheckMark" Grid.Column="0" Data="M 4,8 L 8,12 L 14,4" Stroke="{StaticResource Brush.Accent}" StrokeThickness="2" Visibility="Hidden" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                
                                <!-- Content -->
                                <ContentPresenter Grid.Column="1" ContentSource="Header" VerticalAlignment="Center" Margin="8,0"/>
                                
                                <!-- Shortcut -->
                                <TextBlock Grid.Column="2" Text="{TemplateBinding InputGestureText}" VerticalAlignment="Center" Foreground="{StaticResource Brush.Text.Secondary}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Height="1" Background="{StaticResource Brush.Control.Border}" Margin="4,2"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*" MinHeight="200"/> <!-- Area B: S Curve (Main) -->
            <RowDefinition Height="Auto"/>               <!-- Splitter -->
            <RowDefinition Height="1.5*" MinHeight="150"/> <!-- Area C: V/A/J Curves (Secondary) -->
        </Grid.RowDefinitions>

        <!-- Area B: S Curve (Displacement) -->
        <Border Grid.Row="0" Background="{StaticResource Brush.PlotBackground}" CornerRadius="2" Margin="0,0,0,0">
            <Grid>
                <oxy:PlotView Model="{Binding SPlotModel}" Background="Transparent" DefaultTrackerTemplate="{StaticResource DarkTrackerTemplate}">
                    <oxy:PlotView.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Reset View" Command="{Binding ResetViewCommand}"/>
                        </ContextMenu>
                    </oxy:PlotView.ContextMenu>
                </oxy:PlotView>
                
                <!-- HUD Overlay -->
                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Top" Margin="20" Orientation="Horizontal">
                    <Border Width="12" Height="12" Background="{StaticResource Brush.Accent}" CornerRadius="6" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Displacement (S)" Foreground="{StaticResource Brush.Accent}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Splitter (Horizontal) -->
        <GridSplitter Grid.Row="1" Height="6" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="Transparent" ShowsPreview="True"/>

        <!-- Area C: V/A/J Curves (Derivatives) -->
        <Border Grid.Row="2" Background="{StaticResource Brush.PlotBackground}" CornerRadius="2">
            <Grid>
                <oxy:PlotView Model="{Binding VAPlotModel}" Background="Transparent" DefaultTrackerTemplate="{StaticResource DarkTrackerTemplate}">
                    <oxy:PlotView.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Reset View" Command="{Binding ResetViewCommand}"/>
                            <Separator/>
                            <MenuItem Header="Velocity" IsCheckable="True" IsChecked="{Binding IsVelocityVisible}"/>
                            <MenuItem Header="Acceleration" IsCheckable="True" IsChecked="{Binding IsAccelerationVisible}"/>
                            <MenuItem Header="Jerk" IsCheckable="True" IsChecked="{Binding IsJerkVisible}"/>
                        </ContextMenu>
                    </oxy:PlotView.ContextMenu>
                </oxy:PlotView>
                
                <!-- HUD Overlay -->
                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Top" Margin="20" Background="#80111111">
                     <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <Border Width="10" Height="10" Background="{StaticResource Brush.Success}" CornerRadius="5" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Velocity (V)" Foreground="{StaticResource Brush.Success}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <Border Width="10" Height="10" Background="{StaticResource Brush.Warning}" CornerRadius="5" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Acceleration (A)" Foreground="{StaticResource Brush.Warning}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Border Width="10" Height="10" Background="#9B59B6" CornerRadius="5" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Jerk (J)" Foreground="#9B59B6" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
