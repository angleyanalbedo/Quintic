<UserControl x:Class="Quintic.Wpf.Views.SegmentTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:core="clr-namespace:Quintic.Wpf.Core.Models"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">
    
    <UserControl.Resources>
        <ObjectDataProvider x:Key="MotionLawEnum" MethodName="GetValues" ObjectType="{x:Type system:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="core:MotionLawType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
            <Setter Property="Background" Value="{StaticResource Brush.Control.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Brush.Control.Border}"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="{StaticResource Brush.Control.Background}"/>
                        <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#44FFFFFF"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HeaderButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#11FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{StaticResource Brush.Control.Background}" CornerRadius="2" Padding="0">
        <DockPanel>
            <!-- Header Strip -->
            <Border DockPanel.Dock="Top" Background="{StaticResource Brush.Control.Border}" Padding="10,6" CornerRadius="2,2,0,0">
                <DockPanel>
                     <TextBlock Text="SEGMENT TABLE" FontSize="12" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Secondary}" VerticalAlignment="Center" DockPanel.Dock="Left"/>
                     <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                         <Button Command="{Binding AddSegmentCommand}" Content="+" Style="{StaticResource HeaderButtonStyle}" Margin="5,0,0,0" ToolTip="Add Segment"/>
                         <Button Command="{Binding RemoveSegmentCommand}" Content="-" Style="{StaticResource HeaderButtonStyle}" Margin="0,0,0,0" ToolTip="Remove Segment"/>
                     </StackPanel>
                </DockPanel>
            </Border>
            
            <DataGrid ItemsSource="{Binding Segments}" 
                      SelectedItem="{Binding SelectedSegment}"
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      CanUserDeleteRows="False"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      BorderThickness="0"
                      RowHeight="28"
                      Background="Transparent">
                <DataGrid.Resources>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#33FFFFFF"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#22FFFFFF"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFFFFFFF"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="#FFFFFFFF"/>
                    <Style TargetType="TextBox">
                        <Setter Property="Background" Value="#22FFFFFF"/>
                        <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="2"/>
                        <Setter Property="CaretBrush" Value="{StaticResource Brush.Text.Primary}"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#33FFFFFF"/>
                                <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#11FFFFFF"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding IsLimitExceeded}" Value="True">
                                <Setter Property="Background" Value="#40FF0000"/>
                                <Setter Property="ToolTip" Value="Physical Limit Exceeded (Velocity or Acceleration)"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Insert Segment" Command="{Binding AddSegmentCommand}"/>
                        <MenuItem Header="Remove Segment" Command="{Binding RemoveSegmentCommand}"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.Columns>
                    <!-- Master Start (Computed, ReadOnly) -->
                    <DataGridTextColumn Binding="{Binding ComputedMasterStart, StringFormat=F1}" Width="0.7*" IsReadOnly="True" Foreground="Gray">
                        <DataGridTextColumn.Header>
                            <TextBlock Text="M.Start" ToolTip="Master Start Angle (deg) - Calculated automatically"/>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>

                    <!-- Master End (Editable) -->
                    <DataGridTextColumn Binding="{Binding MasterVal, StringFormat=F1}" Width="0.7*">
                        <DataGridTextColumn.Header>
                            <TextBlock Text="M.End" ToolTip="Master End Angle (deg) or Duration"/>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>

                    <!-- Slave Start (Computed, ReadOnly) -->
                    <DataGridTextColumn Binding="{Binding ComputedSlaveStart, StringFormat=F1}" Width="0.7*" IsReadOnly="True" Foreground="Gray">
                        <DataGridTextColumn.Header>
                            <TextBlock Text="S.Start" ToolTip="Slave Start Position (mm) - Calculated automatically"/>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>

                    <!-- Slave End (Editable) -->
                    <DataGridTextColumn Binding="{Binding SlaveVal, StringFormat=F1}" Width="0.7*">
                        <DataGridTextColumn.Header>
                            <TextBlock Text="S.End" ToolTip="Slave End Position (mm) or Stroke"/>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>

                    <!-- Motion Law -->
                    <DataGridComboBoxColumn Width="1.5*" SelectedItemBinding="{Binding MotionLaw}" ItemsSource="{Binding Source={StaticResource MotionLawEnum}}">
                        <DataGridComboBoxColumn.Header>
                            <TextBlock Text="Law" ToolTip="Motion Law / Curve Type"/>
                        </DataGridComboBoxColumn.Header>
                        <DataGridComboBoxColumn.ElementStyle>
                            <Style TargetType="ComboBox" BasedOn="{StaticResource DarkComboBoxStyle}">
                                <Setter Property="IsHitTestVisible" Value="False"/>
                                <Setter Property="Focusable" Value="False"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Background" Value="Transparent"/>
                            </Style>
                        </DataGridComboBoxColumn.ElementStyle>
                        <DataGridComboBoxColumn.EditingElementStyle>
                            <Style TargetType="ComboBox" BasedOn="{StaticResource DarkComboBoxStyle}"/>
                        </DataGridComboBoxColumn.EditingElementStyle>
                    </DataGridComboBoxColumn>

                    <!-- Edit Control Points Button -->
                    <DataGridTemplateColumn Width="40">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.EditControlPointsCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        CommandParameter="{Binding}"
                                        Content="..." 
                                        ToolTip="Edit Control Points"
                                        Style="{StaticResource HeaderButtonStyle}"
                                        HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Border>
</UserControl>
