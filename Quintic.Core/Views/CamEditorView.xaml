<UserControl x:Class="Quintic.Wpf.Views.CamEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:Quintic.Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1280">
    
    <UserControl.Resources>
        <Style TargetType="ContextMenu">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Grid.IsSharedSizeScope" Value="true"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{StaticResource Brush.Control.Background}" BorderThickness="1" BorderBrush="{StaticResource Brush.Control.Border}" Padding="2">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border" Background="Transparent" Padding="6,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20" SharedSizeGroup="Icon"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Checkmark for IsCheckable items -->
                                <Path x:Name="CheckMark" Grid.Column="0" Data="M 4,8 L 8,12 L 14,4" Stroke="{StaticResource Brush.Accent}" StrokeThickness="2" Visibility="Hidden" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                
                                <!-- Content -->
                                <ContentPresenter Grid.Column="1" ContentSource="Header" VerticalAlignment="Center" Margin="8,0"/>
                                
                                <!-- Shortcut -->
                                <TextBlock Grid.Column="2" Text="{TemplateBinding InputGestureText}" VerticalAlignment="Center" Foreground="{StaticResource Brush.Text.Secondary}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Height="1" Background="{StaticResource Brush.Control.Border}" Margin="4,2"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Toolbar -->
            <RowDefinition Height="*"/>    <!-- Content -->
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <views:ToolbarView Grid.Row="0" DataContext="{Binding ToolbarVM}"/>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="5">
            <Grid.ColumnDefinitions>
                <!-- Left Sidebar: Data Grid (Area A) -->
                <ColumnDefinition Width="420" MinWidth="300" MaxWidth="600"/>
                <!-- Splitter -->
                <ColumnDefinition Width="Auto"/>
                <!-- Right Content: Plot Area -->
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Area A: Left Sidebar (Data Grid + Limits) -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <views:SegmentTableView Grid.Row="0" DataContext="{Binding SegmentTableVM}"/>

                <!-- Splitter -->
                <GridSplitter Grid.Row="1" Height="6" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="Transparent" ShowsPreview="True"/>

                <!-- Logic Tracks Management -->
                <Border Grid.Row="2" Background="{StaticResource Brush.Control.Background}" Margin="0,0,0,5" Padding="5" CornerRadius="2">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <DockPanel Grid.Row="0" Margin="0,0,0,5">
                            <TextBlock Text="LOGIC TRACKS" FontSize="12" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Secondary}" VerticalAlignment="Center"/>
                            <Button Content="+" Command="{Binding LogicTracksVM.AddTrackCommand}" Width="24" Height="24" HorizontalAlignment="Right" Background="Transparent" Foreground="{StaticResource Brush.Accent}" BorderThickness="0" FontWeight="Bold" FontSize="16"/>
                        </DockPanel>

                        <DataGrid Grid.Row="1" ItemsSource="{Binding LogicTracksVM.Tracks}" AutoGenerateColumns="False" CanUserAddRows="False" 
                                  Background="Transparent" RowBackground="Transparent" BorderThickness="0" HeadersVisibility="Column">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding ChannelIndex}" Width="40" IsReadOnly="True" Foreground="Gray"/>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" Foreground="White"/>
                                <DataGridTemplateColumn Width="30">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="×" Command="{Binding DataContext.LogicTracksVM.RemoveTrackCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                    CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Foreground="#E74C3C"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>

                <!-- Physical Limits Configuration -->
                <Border Grid.Row="3" Background="{StaticResource Brush.Control.Background}" Margin="0,0,0,0" Padding="10" CornerRadius="2">
                    <StackPanel>
                        <TextBlock Text="PHYSICAL LIMITS" FontSize="12" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,0,0,10"/>
                        
                        <Grid Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Max Velocity" VerticalAlignment="Center" Foreground="{StaticResource Brush.Text.Primary}" ToolTip="Maximum allowed velocity (mm/s)"/>
                            <TextBox Grid.Column="1" Text="{Binding LimitVelocity, UpdateSourceTrigger=LostFocus}" Padding="4,2" Background="#222" Foreground="White" BorderThickness="0" VerticalContentAlignment="Center"/>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Max Accel" VerticalAlignment="Center" Foreground="{StaticResource Brush.Text.Primary}" ToolTip="Maximum allowed acceleration (mm/s²)"/>
                            <TextBox Grid.Column="1" Text="{Binding LimitAcceleration, UpdateSourceTrigger=LostFocus}" Padding="4,2" Background="#222" Foreground="White" BorderThickness="0" VerticalContentAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Splitter (Vertical) -->
            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" ShowsPreview="True"/>

            <!-- Right Content: Plots -->
            <views:CamPlotView Grid.Column="2" DataContext="{Binding CamPlotVM}"/>
        </Grid>
    </Grid>
</UserControl>
