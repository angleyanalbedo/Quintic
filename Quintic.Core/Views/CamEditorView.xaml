<UserControl x:Class="Quintic.Wpf.Views.CamEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:Quintic.Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1280">
    
    <UserControl.Resources>
        <Style TargetType="ContextMenu">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Grid.IsSharedSizeScope" Value="true"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{StaticResource Brush.Control.Background}" BorderThickness="1" BorderBrush="{StaticResource Brush.Control.Border}" Padding="2">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border" Background="Transparent" Padding="6,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20" SharedSizeGroup="Icon"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Checkmark for IsCheckable items -->
                                <Path x:Name="CheckMark" Grid.Column="0" Data="M 4,8 L 8,12 L 14,4" Stroke="{StaticResource Brush.Accent}" StrokeThickness="2" Visibility="Hidden" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                
                                <!-- Content -->
                                <ContentPresenter Grid.Column="1" ContentSource="Header" VerticalAlignment="Center" Margin="8,0"/>
                                
                                <!-- Shortcut -->
                                <TextBlock Grid.Column="2" Text="{TemplateBinding InputGestureText}" VerticalAlignment="Center" Foreground="{StaticResource Brush.Text.Secondary}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Height="1" Background="{StaticResource Brush.Control.Border}" Margin="4,2"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Dark TabControl Style -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0,5,0,0"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Secondary}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
                                <Setter Property="BorderBrush" Value="{StaticResource Brush.Accent}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Toolbar -->
            <RowDefinition Height="*"/>    <!-- Content -->
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <views:ToolbarView Grid.Row="0" DataContext="{Binding ToolbarVM}"/>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="5">
            <Grid.ColumnDefinitions>
                <!-- Left Sidebar: Data Grid (Area A) -->
                <ColumnDefinition Width="420" MinWidth="300" MaxWidth="600"/>
                <!-- Splitter -->
                <ColumnDefinition Width="Auto"/>
                <!-- Right Content: Plot Area -->
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Area A: Left Sidebar -->
            <TabControl Grid.Column="0" Margin="0,0,5,0">
                <!-- Tab 1: Motion Profile -->
                <TabItem Header="MOTION PROFILE">
                    <Grid Margin="0,5,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <views:SegmentTableView Grid.Row="0" DataContext="{Binding SegmentTableVM}"/>

                        <!-- Physical Limits Configuration -->
                        <Border Grid.Row="1" Background="{StaticResource Brush.Control.Background}" Margin="0,10,0,0" Padding="15" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="PHYSICAL LIMITS" FontSize="12" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,0,0,12"/>
                                
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="100"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Max Velocity:" VerticalAlignment="Center" Foreground="{StaticResource Brush.Text.Primary}" ToolTip="Maximum allowed velocity (mm/s)"/>
                                    <TextBox Grid.Column="1" Text="{Binding LimitVelocity, UpdateSourceTrigger=LostFocus}" Padding="6,4" Background="#222" Foreground="White" BorderThickness="0" VerticalContentAlignment="Center"/>
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="100"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Max Accel:" VerticalAlignment="Center" Foreground="{StaticResource Brush.Text.Primary}" ToolTip="Maximum allowed acceleration (mm/s²)"/>
                                    <TextBox Grid.Column="1" Text="{Binding LimitAcceleration, UpdateSourceTrigger=LostFocus}" Padding="6,4" Background="#222" Foreground="White" BorderThickness="0" VerticalContentAlignment="Center"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </Grid>
                </TabItem>

                <!-- Tab 2: Logic Tracks -->
                <TabItem Header="LOGIC TRACKS">
                    <Grid Margin="0,5,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="1.5*"/>
                        </Grid.RowDefinitions>

                        <!-- Logic Tracks Management -->
                        <Border Grid.Row="0" Background="{StaticResource Brush.Control.Background}" Padding="10" CornerRadius="4">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <DockPanel Grid.Row="0" Margin="0,0,0,10">
                                    <TextBlock Text="OUTPUT CHANNELS" FontSize="12" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Secondary}" VerticalAlignment="Center"/>
                                    <Button Content="+" Command="{Binding LogicTracksVM.AddTrackCommand}" Width="24" Height="24" HorizontalAlignment="Right" Background="Transparent" Foreground="{StaticResource Brush.Accent}" BorderThickness="0" FontWeight="Bold" FontSize="16"/>
                                </DockPanel>

                                <DataGrid Grid.Row="1" ItemsSource="{Binding LogicTracksVM.Tracks}" SelectedItem="{Binding LogicTracksVM.SelectedTrack}" AutoGenerateColumns="False" CanUserAddRows="False" 
                                          Background="Transparent" RowBackground="Transparent" BorderThickness="0" HeadersVisibility="Column">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="CH" Binding="{Binding ChannelIndex}" Width="40" IsReadOnly="True" Foreground="Gray"/>
                                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" Foreground="White">
                                            <DataGridTextColumn.EditingElementStyle>
                                                <Style TargetType="TextBox">
                                                    <Setter Property="Background" Value="#2D2D2D"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                    <Setter Property="BorderThickness" Value="0"/>
                                                    <Setter Property="Padding" Value="2"/>
                                                </Style>
                                            </DataGridTextColumn.EditingElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTemplateColumn Width="30">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="×" Command="{Binding DataContext.LogicTracksVM.RemoveTrackCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                            CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Foreground="#E74C3C"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Border>

                        <!-- Splitter -->
                        <GridSplitter Grid.Row="1" Height="8" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="Transparent" ShowsPreview="True"/>

                        <!-- Track Switches Configuration -->
                        <Border Grid.Row="2" Background="{StaticResource Brush.Control.Background}" Padding="10" CornerRadius="4">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <DockPanel Grid.Row="0" Margin="0,0,0,10">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="SWITCH INTERVALS" FontSize="12" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Secondary}"/>
                                        <TextBlock Text="{Binding LogicTracksVM.SelectedTrack.Name, StringFormat=' - {0}'}" FontSize="12" FontWeight="Bold" Foreground="{StaticResource Brush.Accent}"/>
                                    </StackPanel>
                                    <Button Content="+" Command="{Binding LogicTracksVM.AddSwitchCommand}" Width="24" Height="24" HorizontalAlignment="Right" Background="Transparent" Foreground="{StaticResource Brush.Accent}" BorderThickness="0" FontWeight="Bold" FontSize="16"/>
                                </DockPanel>

                                <DataGrid Grid.Row="1" ItemsSource="{Binding LogicTracksVM.SelectedTrack.Switches}" AutoGenerateColumns="False" CanUserAddRows="False" 
                                          Background="Transparent" RowBackground="Transparent" BorderThickness="0" HeadersVisibility="Column">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="On Angle (°)" Binding="{Binding OnAngle, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:F1}}" Width="*" Foreground="{StaticResource Brush.Success}">
                                            <DataGridTextColumn.EditingElementStyle>
                                                <Style TargetType="TextBox">
                                                    <Setter Property="Background" Value="#2D2D2D"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                    <Setter Property="BorderThickness" Value="0"/>
                                                </Style>
                                            </DataGridTextColumn.EditingElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTextColumn Header="Off Angle (°)" Binding="{Binding OffAngle, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:F1}}" Width="*" Foreground="{StaticResource Brush.Warning}">
                                            <DataGridTextColumn.EditingElementStyle>
                                                <Style TargetType="TextBox">
                                                    <Setter Property="Background" Value="#2D2D2D"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                    <Setter Property="BorderThickness" Value="0"/>
                                                </Style>
                                            </DataGridTextColumn.EditingElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTemplateColumn Width="30">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="×" Command="{Binding DataContext.LogicTracksVM.RemoveSwitchCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                            CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Foreground="#E74C3C"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                                
                                <!-- Empty state hint -->
                                <TextBlock Grid.Row="1" Text="Select a track above to edit its switches." Foreground="Gray" HorizontalAlignment="Center" VerticalAlignment="Center" FontStyle="Italic">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding LogicTracksVM.SelectedTrack}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Border>
                    </Grid>
                </TabItem>
            </TabControl>

            <!-- Splitter (Vertical) -->
            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" ShowsPreview="True"/>

            <!-- Right Content: Plots -->
            <views:CamPlotView Grid.Column="2" DataContext="{Binding CamPlotVM}"/>
        </Grid>
    </Grid>
</UserControl>
