<Window x:Class="Quintic.Wpf.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Quintic.Wpf.ViewModels"
        xmlns:views="clr-namespace:Quintic.Wpf.Views"
        mc:Ignorable="d"
        Title="Quintic - Advanced Cam Editor" Height="768" Width="1280"
        Background="{StaticResource Brush.Background}"
        Foreground="{StaticResource Brush.Text.Primary}"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <!-- Left Sidebar: Data Grid (Area A) -->
            <ColumnDefinition Width="420" MinWidth="300" MaxWidth="600"/>
            <!-- Splitter -->
            <ColumnDefinition Width="Auto"/>
            <!-- Right Content: Plot Area -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Area A: Data Grid (Left Sidebar) -->
        <views:SegmentTableView Grid.Column="0"/>

        <!-- Splitter (Vertical) -->
        <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" ShowsPreview="True"/>

        <!-- Right Content: Plots -->
        <views:CamPlotView Grid.Column="2"/>

    </Grid>
</Window>
