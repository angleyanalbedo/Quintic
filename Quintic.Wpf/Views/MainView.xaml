<Window x:Class="Quintic.Wpf.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Quintic.Wpf.ViewModels"
        xmlns:views="clr-namespace:Quintic.Wpf.Views"
        mc:Ignorable="d"
        Title="Quintic - Advanced Cam Editor" Height="768" Width="1280"
        Background="{StaticResource Brush.Background}"
        Foreground="{StaticResource Brush.Text.Primary}"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Toolbar -->
            <RowDefinition Height="*"/>    <!-- Content -->
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <views:ToolbarView Grid.Row="0" DataContext="{Binding ToolbarVM}"/>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="5">
            <Grid.ColumnDefinitions>
                <!-- Left Sidebar: Data Grid (Area A) -->
                <ColumnDefinition Width="420" MinWidth="300" MaxWidth="600"/>
                <!-- Splitter -->
                <ColumnDefinition Width="Auto"/>
                <!-- Right Content: Plot Area -->
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Area A: Left Sidebar (Data Grid + Limits) -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <views:SegmentTableView Grid.Row="0" DataContext="{Binding SegmentTableVM}"/>

                <!-- Physical Limits Configuration -->
                <Border Grid.Row="1" Background="{StaticResource Brush.Control.Background}" Margin="0,5,0,0" Padding="10" CornerRadius="2">
                    <StackPanel>
                        <TextBlock Text="PHYSICAL LIMITS" FontSize="12" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,0,0,10"/>
                        
                        <Grid Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Max Velocity" VerticalAlignment="Center" Foreground="{StaticResource Brush.Text.Primary}" ToolTip="Maximum allowed velocity (mm/s)"/>
                            <TextBox Grid.Column="1" Text="{Binding LimitVelocity, UpdateSourceTrigger=PropertyChanged}" Padding="4,2" Background="#222" Foreground="White" BorderThickness="0" VerticalContentAlignment="Center"/>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Max Accel" VerticalAlignment="Center" Foreground="{StaticResource Brush.Text.Primary}" ToolTip="Maximum allowed acceleration (mm/s²)"/>
                            <TextBox Grid.Column="1" Text="{Binding LimitAcceleration, UpdateSourceTrigger=PropertyChanged}" Padding="4,2" Background="#222" Foreground="White" BorderThickness="0" VerticalContentAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Splitter (Vertical) -->
            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" ShowsPreview="True"/>

            <!-- Right Content: Plots -->
            <views:CamPlotView Grid.Column="2" DataContext="{Binding CamPlotVM}"/>
        </Grid>
    </Grid>
</Window>
