<UserControl x:Class="Quintic.Wpf.Views.ToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="800">
    <Border Background="{StaticResource Brush.Control.Background}" BorderBrush="{StaticResource Brush.Control.Border}" BorderThickness="0,0,0,1" Padding="10,0">
        <Grid>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Quintic" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Margin="0,0,20,0" Foreground="{StaticResource Brush.Accent}"/>
                
                <!-- Export Menu -->
                <Menu Background="Transparent" VerticalAlignment="Center">
                    <Menu.Resources>
                        <Style TargetType="MenuItem">
                            <Setter Property="Background" Value="{StaticResource Brush.Control.Background}"/>
                            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
                        </Style>
                    </Menu.Resources>
                    <MenuItem Header="Export" Foreground="{StaticResource Brush.Text.Primary}" FontSize="14" Padding="10,5" Background="Transparent">
                        <MenuItem.Resources>
                            <Style TargetType="Popup">
                                <Setter Property="AllowsTransparency" Value="True"/>
                            </Style>
                        </MenuItem.Resources>
                        <MenuItem Header="To CSV..." Command="{Binding ExportCsvCommand}"/>
                        <!-- Future formats can be added here, e.g.: -->
                        <!-- <MenuItem Header="To JSON..." Command="{Binding ExportJsonCommand}"/> -->
                    </MenuItem>
                </Menu>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
